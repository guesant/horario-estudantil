{
  "name": "@horario-estudantil/schemas",
  "version": "4.0.0-beta",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch": "tsc -p . --watch",
    "build": "npm run clear && tsc -p .",
    "prepare": "npm run build",
    "clear": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "prettier": "^2.7.1",
    "typescript": "^4.9.4"
  },
  "peerDependencies": {
    "zod": "^3.20.2"
  },
  "files": [
    "/dist"
  ]
}
