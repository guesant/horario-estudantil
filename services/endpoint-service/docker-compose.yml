services:
  endpoint:
    image: node:16
    container_name: he-endpoint
    restart: always
    command: sh -c "tail -f /dev/null"
    env_file:
      - .env
    ports:
      - 3001:3001
    user: node
    working_dir: /code
    volumes:
      - .:/code
    networks:
      - he-net

networks:
  he-net:
    external: true